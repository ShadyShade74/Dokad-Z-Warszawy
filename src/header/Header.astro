<header>
    <div class="header-container">
        <div class="header-options">
            <a href="#"><div class="header-option" id="main">Strona Główna</div></a>
            <a href="#"><div class="header-option" id="short">Wycieczki Jednioniowe</div></a>
            <a href="#"><div class="header-option" id="long">Dłuższe Wycieczki</div></a>
            <a href="#"><div class="header-option" id="about">O blogu</div></a>
        </div>
    </div>
</header>
<style>
    header{
        width:100%;
        height:75px;
        display:flex;
        justify-content: flex-end;
    }
    .header-container{
        display: flex;
        justify-content:flex-end ;
        height:75px;
        margin-right:45px;
    }

    .header-options{
        display: flex;
        justify-content: space-between;
        gap:25px;

    }
    .header-option{
        font-family:'Sour Gummy', sans-serif;
        color: #000;
        font-size:25px;
        display:flex;
        align-items: center;
        height: 100%;
        justify-content: center;
        transition: all 0.2s ease-in-out;
     }
    .header-option:hover{
        color:rgb(43, 140, 200);
        transition:all 0.2s ease-in-out;
    }
    .header-options a{
        text-decoration: none;
        color: #000000;
    }
</style>
<script>   
            document.addEventListener('DOMContentLoaded', function() {
        const path = window.location.pathname;
        
        if (path === '/Dokad-Z-Warszawy/blog.html' || path === '/blog.html') {
            const mainOption = document.getElementById('about');
            if (mainOption) {
                mainOption.style.color = 'rgb(43, 140, 200)';
            }
        }
        
        if (path === '/Dokad-Z-Warszawy/index.html' || path === '/index.html' || path === '/Dokad-Z-Warszawy/' || path === '/') {
            const mainOption = document.getElementById('main');
            if (mainOption) {
                mainOption.style.color = 'rgb(43, 140, 200)';
            }
        }
    });
</script>